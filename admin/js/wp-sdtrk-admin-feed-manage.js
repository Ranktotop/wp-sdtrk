/**
 * Product-feed management page.
 *
 * Loads published products (paginated, server-side search) and lets the admin
 * toggle each one between "in feed" and "excluded", persisting the change via
 * the save_feed_exclusion AJAX endpoint. Wired up in T7/T9.
 */
(function ($) {
    'use strict';
    // Implemented in T7 (list/render) and T9 (toggle/bulk save).
})(jQuery);
